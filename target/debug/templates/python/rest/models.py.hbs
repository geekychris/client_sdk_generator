"""
{{api_name}} Python SDK Models
Generated by client-sdk-generator

Data models for {{api_name}}
"""

from dataclasses import dataclass
from typing import Dict, List, Optional, Any
import json


{{#each types}}
@dataclass
class {{name}}:
    """
    {{#if description}}{{description}}{{else}}{{name}} model{{/if}}
    """
    {{#each properties}}
    {{snake_case name}}: {{python_type data_type}}{{#unless required}} = None{{/unless}}
    {{/each}}
    
    @classmethod
    def from_dict(cls, data: Dict[str, Any]) -> '{{name}}':
        """Create {{name}} instance from dictionary"""
        return cls(
            {{#each properties}}
            {{snake_case name}}=data.get('{{name}}'){{#unless @last}},{{/unless}}
            {{/each}}
        )
    
    def to_dict(self) -> Dict[str, Any]:
        """Convert {{name}} instance to dictionary"""
        result = {}
        {{#each properties}}
        if self.{{snake_case name}} is not None:
            result['{{name}}'] = self.{{snake_case name}}
        {{/each}}
        return result
    
    def to_json(self) -> str:
        """Convert {{name}} instance to JSON string"""
        return json.dumps(self.to_dict())

{{/each}}