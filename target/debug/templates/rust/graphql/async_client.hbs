// This file contains the async client implementation.
// For Rust, async support is typically built into the main client
// since Rust has native async/await support.

// This template is primarily a placeholder as async functionality
// is typically integrated directly into the main client in Rust
pub use crate::client::{{class_name spec.name}}Client as {{class_name spec.name}}AsyncClient;