/**
 * {{api_name}} TypeScript SDK
 * 
 * {{#if description}}{{description}}{{/if}}
 * 
 * @version {{api_version}}
 * @generated by client-sdk-generator
 */

export { {{class_name}} } from './client';
export type { 
  {{#if authentication}}
  AuthConfig,
  {{/if}}
  ClientConfig
} from './client';

export type {
  {{#each types}}
  {{name}},
  {{/each}}
  APIError,
  HttpMethod,
  RequestOptions,
  PaginationMeta,
  PaginatedResponse,
} from './types';

export { APIClientError } from './types';

// Default export for convenience
export { {{class_name}} as default } from './client';